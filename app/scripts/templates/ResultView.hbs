<div class="result-panel">
  <div class="panel panel-default">
    <div class="panel-heading">Hello <span class="username">{{#if user.name}}{{user.name}}{{else}}nobody{{/if}}</span>, check your results!</div>
    <div class="panel-body">
      <ul class="list-group"></ul>
    </div>
    <div class="panel-footer">
      Your final result is: <strong>{{correctAnswers}} of {{totalQuestions}}</strong>
      <div class="progress">
        <div class="progress-bar" role="progressbar" aria-valuenow="40" aria-valuemin="0" aria-valuemax="100" style=""></div>
      </div>
      <div class="text-center">
        <button id="try-again-btn" class="btn btn-large">Try again</button>
      </div>
    </div>
  </div>
</div>